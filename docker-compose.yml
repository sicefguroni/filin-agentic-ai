services:
  # 1. AI Agent
  financial-agent:
    build: .
    volumes:
      - ./src:/app/src
      - ./data:/app/data
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - TAVILY_API_KEY=${TAVILY_API_KEY}
    command: tail -f /dev/null
